# BLOAD at $6000: jmp + params $6003..7, code, rodata, DATA (wk + bench_arr).
MEMORY {
    RAM: file = %O, define = yes, start = $6000, size = $0FE0;
}
SEGMENTS {
    CODE:   load = RAM, type = ro;
    RODATA: load = RAM, type = ro;
    DATA:   load = RAM, type = rw;
}
